import { Component } from '@angular/core';

/**
 * ThreeSceneComponent — stub
 * The original implementation depended on `three` and `three-stdlib`,
 * which are not declared in package.json.
 * 3D views are handled via Spline iframes in the home component.
 */
@Component({
  selector: 'app-three-scene',
  standalone: true,
  template: '',
  styles: []
})
export class ThreeSceneComponent {}
